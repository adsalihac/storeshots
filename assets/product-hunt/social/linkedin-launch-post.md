We built Shotify for Expo teams that are tired of manually creating store screenshots.

The workflow is intentionally small:

```bash
npx shotify
```

Shotify discovers Expo Router routes, captures screenshots, and exports App Store and Play Store assets for supported device sizes.

It is designed as developer infrastructure: TypeScript-first, CI-friendly, and predictable enough to use in release pipelines.
