{
  "name": "@shotify/core",
  "version": "0.1.0",
  "description": "Core Shotify screenshot generation pipeline.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@shotify/device-presets": "workspace:*",
    "@shotify/expo-router-discovery": "workspace:*",
    "@shotify/image-processor": "workspace:*",
    "@shotify/screenshot-engine": "workspace:*",
    "@shotify/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
